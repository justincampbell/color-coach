const CONFIG = {
    // List of available colors
    colors: [
        // 'red',
        // 'green', 
        'blue',
        'yellow',
        // 'orange',
        // 'purple'
    ],

    // Time between color changes in milliseconds (default: 5 seconds)
    changeInterval: 5000,

    // Display the color name text (true/false)
    showColorName: false,

    // Maximum screen brightness (true/false)
    maxBrightness: true
};
